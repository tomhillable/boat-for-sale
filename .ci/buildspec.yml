version: 0.2

phases:

  install:
    commands:
      - gem uninstall bundler
      - command bundle
      - which bundle
      - bundle --version
      - gem install bundler
      - bundle --version
      - bundle install

  build:
    commands:
      - jekyll build

